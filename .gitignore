node_modules/
.env
.env.local
.env*.local
dist/
.next/
next-env.d.ts
.vercel/
*.tsbuildinfo
*.log
.DS_Store
# Worker output dirs — root-anchored so they don't clash with source dirs
# (e.g. the dashboard/clips route). Do NOT use bare `clips/`.
/tmp/
/clips/
/vods/
/apps/worker/clips/
/apps/worker/vods/
/apps/worker/tmp/
